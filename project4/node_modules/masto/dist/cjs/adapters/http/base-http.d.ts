import { type Encoding, type Http, type HttpMetaParams, type HttpRequestParams, type HttpRequestResult } from "../../interfaces/index.js";
export declare abstract class BaseHttp implements Http {
    abstract request(params: HttpRequestParams): Promise<HttpRequestResult>;
    get<T>(path: string, data?: unknown, meta?: HttpMetaParams<Encoding>): Promise<T>;
    post<T>(path: string, data?: unknown, meta?: HttpMetaParams<Encoding>): Promise<T>;
    delete<T>(path: string, data?: unknown, meta?: HttpMetaParams<Encoding>): Promise<T>;
    put<T>(path: string, data?: unknown, meta?: HttpMetaParams<Encoding>): Promise<T>;
    patch<T>(path: string, data?: unknown, meta?: HttpMetaParams<Encoding>): Promise<T>;
}
