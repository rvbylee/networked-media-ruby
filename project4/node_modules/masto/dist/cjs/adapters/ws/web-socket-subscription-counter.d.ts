import { type WebSocketSubscriptionCounter } from "../../interfaces/index.js";
export declare class WebSocketSubscriptionCounterImpl implements WebSocketSubscriptionCounter {
    private counts;
    count(stream: string, params?: Record<string, unknown>): number;
    increment(stream: string, params?: Record<string, unknown>): void;
    decrement(stream: string, params?: Record<string, unknown>): void;
    private hash;
}
